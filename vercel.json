{
  "buildCommand": "cd packages/db && npm install && npx prisma generate && cd ../../apps/web && npm run build",
  "devCommand": "cd apps/web && npm run dev",
  "installCommand": "pnpm install && cd apps/web && npm install",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}